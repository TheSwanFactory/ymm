# - install
# - test
# - watch
# - release
# - dev
init:
  KEY: value
  DICT:
    A: B
    C: D

install:
    - install test

call:
    - . echo

echo:
    RESULT: $ echo {DICT}

python:
    - _ 2 + 2

pvar:
    IN: 2 + 2
    OUT: _ {IN}

pipe:
    IN: "Hello"
    OUT: ^ xargs

path:
    IN: "{DICT}"
    OUT: ~ A

run-param:
  JOB: 484
  PARAM: RSERIAL1
  PRINT: databricks jobs run-now --job-id {JOB} --notebook-params '{{"CONF":"{PARAM}","DEBUG":"DEBUG"}}'
  DBRUN: $ databricks jobs run-now --job-id {JOB} --notebook-params {{"CONF":"{PARAM}","DEBUG":"DEBUG"}}
